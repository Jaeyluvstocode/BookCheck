# BookCheck - Client
## Setup
1. In `client` directory run `npm install`
2. Run `npm start`
Frontend expects API at `/api` (if server runs on port 5000, setup a proxy or run client with environment variable)
